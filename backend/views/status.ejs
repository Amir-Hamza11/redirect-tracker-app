<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Redirect Status</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-cyan-700 min-h-screen flex flex-col items-center justify-center p-6">

  <!-- Illustration -->
  <img 
  src="/teamwork.svg"  
  alt="Office Teamwork Illustration"
  class="w-64 mb-6 drop-shadow-lg"
/>

  <!-- Title -->
  <% let message = ""; %>
<% if (status === "complete") { %>
  <% message = "✅ Survey Completed Successfully!" %>
<% } else if (status === "terminate") { %>
  <% message = "❌ Survey Terminated" %>
<% } else if (status === "quotafull") { %>
  <% message = "🚫 Survey Quota Full" %>
<% } else { %>
  <% message = "Survey Status Unknown" %>
<% } %>

<h2 class="text-green-300 text-3xl font-bold mb-6"><%= message %></h2>


  <!-- Table -->
  <div class="bg-white shadow-md rounded-lg overflow-hidden w-full max-w-2xl">
    <table class="table-auto w-full text-left border border-gray-200">
      <thead class="bg-gray-300">
        <tr>
          <th class="px-4 py-3 border-b">PID</th>
          <th class="px-4 py-3 border-b">UID</th>
          <th class="px-4 py-3 border-b">Status</th>
          <th class="px-4 py-3 border-b">IP Address</th>
        </tr>
      </thead>
      <tbody>
        <tr class="hover:bg-gray-100">
          <td class="px-4 py-3 border-b"><%= pid %></td>
          <td class="px-4 py-3 border-b"><%= uid %></td>
          <td class="px-4 py-3 border-b capitalize text-blue-600 font-semibold"><%= status %></td>
          <td class="px-4 py-3 border-b"><%= ip %></td>
        </tr>
      </tbody>
    </table>
  </div>

</body>
</html>
